[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false
ffi = true
fs_permissions = [{ access = "read-write", path = "./" }]

# Remappings
remappings = [
    "@openzeppelin/contracts/=lib/hyper-evm-lib/lib/openzeppelin-contracts/contracts/",
    "@hyper-evm-lib/=lib/hyper-evm-lib/src/",
    "@valantis-core/=lib/valantis-core/src/",
    "forge-std/=lib/hyper-evm-lib/lib/forge-std/src/",
]

# Testing
[profile.default.fuzz]
runs = 256
max_test_rejects = 65536

[profile.default.invariant]
runs = 256
depth = 15

# Fork testing for Hyperliquid
[profile.fork]
fork_url = "https://rpc.hyperliquid-testnet.xyz/evm"
fork_block_number = 0

[rpc_endpoints]
hyperliquid_testnet = "https://rpc.hyperliquid-testnet.xyz/evm"
hyperliquid_mainnet = "https://rpc.hyperliquid.xyz/evm"

[etherscan]
# Hyperliquid doesn't have Etherscan-compatible explorer yet
